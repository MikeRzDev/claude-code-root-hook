#!/bin/sh
# Keep argument boundaries and sudo's exit status. Use the configured password cache; never eval arguments.
set -eu
helper_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
SUDO_ASKPASS="$helper_dir/askpass.sh"
export SUDO_ASKPASS
exec /usr/bin/sudo -A -k "$@"
